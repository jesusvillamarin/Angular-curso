import { ResaltarZoneDirective } from './resaltar-zone.directive';

describe('ResaltarZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarZoneDirective();
    expect(directive).toBeTruthy();
  });
});
